--- ban.rs	2025-01-31 13:00:14.583630079 +0100
+++ ban.new	2025-02-01 17:03:54.620612275 +0100
@@ -64,6 +64,7 @@
 }
 
 async fn ban_player(sender: &CommandSender<'_>, player: &Player, reason: Option<String>) {
+/*
     let mut banned_players = BANNED_PLAYER_LIST.write().await;
 
     let reason = reason.unwrap_or_else(|| "Banned by an operator.".to_string());
@@ -104,6 +105,9 @@
             [].into(),
         ))
         .await;
+*/
+    let msg = TextComponent::text("This command has been disabled on the public server... Sorry!");
+    sender.send_message(msg.color_named(NamedColor::Red)).await;
 }
 
 pub fn init_command_tree() -> CommandTree {
